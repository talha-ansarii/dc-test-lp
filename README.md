# Distance Connect InterAI — Landing Page

## Quick start
```bash
npm install
npm run dev
# open http://localhost:3000
```

## Build & run
```bash
npm run build
npm start
```

## Test
```bash
npm run test
```

## Notes
- Edit content inside `app/page.tsx` `CONTENT` constant.
- OpenGraph image `/public/og.png` and `/public/favicon.ico` are placeholders you can replace.
